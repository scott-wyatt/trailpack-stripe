'use strict'
/* global describe, it */

const assert = require('assert')

describe('Transferreversal Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['Transferreversal'])
  })
})
