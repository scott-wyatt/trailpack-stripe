'use strict'
/* global describe, it */

const assert = require('assert')

describe('Applicationfeerefund Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['Applicationfeerefund'])
  })
})
