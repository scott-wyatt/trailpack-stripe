'use strict'
/* global describe, it */

const assert = require('assert')

describe('Applicationfee Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['Applicationfee'])
  })
})
